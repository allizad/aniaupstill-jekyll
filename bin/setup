#!/bin/bash

cd "$(dirname $0)/.." || exit 1
PATH="./bin:$PATH"

# Install bundler and gems
gem install bundler --conservative
bundle check || bundle install --path .bundle/gems

# Install homebrew packages, if any
if [ -f Brewfile ]; then
  brew bundle check || brew bundle
fi
